---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
	show?: boolean;
}

const { show = true, href } = Astro.props;
---

<div>
	<a href={href}><slot /></a>
	{show && <span>{href}</span>}
</div>
